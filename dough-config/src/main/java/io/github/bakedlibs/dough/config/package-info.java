/**
 * The config module provides means to interact with configuration files.
 * By default only YAML is supported, but this may change in the future as we constantly expand
 * this.
 */
package io.github.bakedlibs.dough.config;